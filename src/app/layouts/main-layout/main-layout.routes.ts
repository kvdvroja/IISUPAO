import { Routes } from '@angular/router';
import { MainLayout } from './main-layout';// Asegúrate de importar tu componente

export const MAIN_LAYOUT: Routes = [
  { path: '', component: MainLayout }
];