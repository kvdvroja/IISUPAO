export interface Plantilla_DestinoI {
  pd_id: string;
  pd_plan_inte_id: string;
  pd_priodridad: string;
  pd_ind_estado: string;
  pd_sist_dest_id: string;
  pd_schema: string;
  pd_sist_id: string;
  pd_url: string;
  pd_metodo_http: string;
  pd_tipo_transformacion: string;
}