import { Routes } from '@angular/router';
import { PlantillaIntegracion } from './plantilla-integracion';

export const PLANTILLA_INTEGRACION_ROUTES: Routes = [
  { path: '', component: PlantillaIntegracion }
];